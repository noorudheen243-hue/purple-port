export class RegularisationWorkflow {
    // Placeholder for future ticket integration
    static async createTicket(requestId: string, reason: string) {
        console.log(`Created Ticket for Regularisation Request ${requestId}: ${reason}`);
    }
}
