const http = require('http'); const data = JSON.stringify({ settings: { TEST: '123' } }); const req = http.request({ hostname: 'localhost', port: 4001, path: '/api/settings/batch', method: 'POST', headers: { 'Content-Type': 'application/json', 'Content-Length': data.length } }, res => { console.log('statusCode:', res.statusCode); res.on('data', d => process.stdout.write(d)); }); req.on('error', console.error); req.write(data); req.end();
